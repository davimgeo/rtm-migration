# General Parameters #

debug = true

# Geometry

create_geom = false

receivers = "data/input/geometry/receivers.txt"
sources = "data/input/geometry/sources.txt" 

offset = 5 # [m]

# Model

load_model = false

model_path = "data/input/models/model_difractor_200x100.bin"

value_interfaces = [1500, 2000]
interfaces = [50]

nx = 200
nz = 100

dh = 10.0

nb     = 100
factor = 0.009

# Seismogram

perc = 99

load_residual = false

residual_input_path = "data/input/residuals/seismogram_nt2001_dt0.001_nrec40.bin"
save_seismogram = true

nt = 2001
dt = 1.0e-3

# Wavelet

fmax = 30.0
tlag = 0.3

# Snapshots

snap_path = "data/snapshots/"

snap_num  = 700
snap_bool = true



